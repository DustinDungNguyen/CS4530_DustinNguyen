
<resources>
    <!-- App theme for the whole application -->
    <style name="Theme.FunFacts" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Optional window customizations -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
